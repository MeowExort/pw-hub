<Window x:Class="Pw.Hub.Pages.CreateSquadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Создать отряд" Height="600" Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Style="{StaticResource ModernWindowChrome}">
    <Border Style="{StaticResource Card}" Margin="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="📁 Создание отряда"
                          Style="{StaticResource HeaderMedium}"
                          HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Form -->
            <StackPanel Grid.Row="1" Margin="0,0,0,20">
                <TextBlock Text="Название отряда"
                          Style="{StaticResource ModernTextBlock}"
                          Margin="0,0,0,6"/>
                <TextBox x:Name="SquadNameTextBox"
                        Style="{StaticResource ModernTextBox}"
                        Padding="12,10"
                        MaxLength="50"/>
            </StackPanel>

            <!-- Actions -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Отмена"
                       Style="{StaticResource ModernButton}"
                       MinWidth="100"
                       Padding="16,10"
                       Margin="0,0,8,0"
                       Click="OnCancelClick"/>
                <Button Content="Создать"
                       Style="{StaticResource PrimaryButton}"
                       MinWidth="100"
                       Padding="16,10"
                       IsDefault="True"
                       Click="OnCreateClick"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
