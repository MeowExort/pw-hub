<Window x:Class="Pw.Hub.Pages.EditAccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Редактировать аккаунт" Height="280" Width="400" 
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Label Content="Название аккаунта:" FontWeight="SemiBold"/>
            <TextBox x:Name="AccountNameTextBox" 
                     Padding="8,6" 
                     FontSize="14"
                     MaxLength="50"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,12,0,0">
            <Label Content="Email (необязательно):" FontWeight="SemiBold"/>
            <TextBox x:Name="EmailTextBox" 
                     Padding="8,6" 
                     FontSize="14"
                     MaxLength="100"/>
        </StackPanel>

        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,16,0,0">
            <Button Content="Отмена" 
                    Padding="24,8" 
                    Margin="0,0,8,0"
                    Click="OnCancelClick"/>
            <Button Content="Сохранить" 
                    Padding="24,8"
                    IsDefault="True"
                    Click="OnSaveClick"/>
        </StackPanel>
    </Grid>
</Window>
