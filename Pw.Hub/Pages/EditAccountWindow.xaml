<Window x:Class="Pw.Hub.Pages.EditAccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Редактировать аккаунт" MinHeight="200" Width="460"
        WindowStartupLocation="CenterOwner"
        SizeToContent="Height"
        Style="{StaticResource ModernWindowChrome}">
    <Border Style="{StaticResource Card}" Margin="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="✏ Редактирование аккаунта"
                          Style="{StaticResource HeaderMedium}"
                          HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Form -->
            <StackPanel Grid.Row="1" Margin="0,0,0,20">
                <TextBlock Text="Название аккаунта"
                          Style="{StaticResource ModernTextBlock}"
                          Margin="0,0,0,6"/>
                <TextBox x:Name="AccountNameTextBox"
                        Style="{StaticResource ModernTextBox}"
                        Padding="12,10"
                        MaxLength="50"
                        Margin="0,0,0,16"/>

                <Border Background="{StaticResource AccentBlueBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="0,1,0,1"
                        Padding="12"
                        Margin="0,8,0,8">
                    <TextBlock Text="ПЕРСОНАЖ ПО УМОЛЧАНИЮ ПО СЕРВЕРАМ"
                               Style="{StaticResource ModernTextBlock}"
                               FontWeight="SemiBold"
                               FontSize="11"
                               Foreground="{StaticResource TextSecondaryBrush}"/>
                </Border>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="ServersList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Vertical" Margin="0,0,0,8">
                                    <TextBlock Text="{Binding Name}"
                                               Style="{StaticResource ModernTextBlock}"
                                               FontWeight="SemiBold"
                                               Margin="0,0,0,6"/>
                                    <ComboBox Style="{StaticResource ModernComboBox}"
                                              ItemsSource="{Binding CharactersWithPlaceholder}"
                                              DisplayMemberPath="Name"
                                              SelectedValuePath="OptionId"
                                              SelectedValue="{Binding DefaultCharacterOptionId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Name}"/>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                        <ComboBox.Resources>
                                            <!-- Placeholder will be inserted from code-behind as first item -->
                                        </ComboBox.Resources>
                                    </ComboBox>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>

            <!-- Actions -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Отмена"
                       Style="{StaticResource ModernButton}"
                       MinWidth="100"
                       Padding="16,10"
                       Margin="0,0,8,0"
                       Click="OnCancelClick"/>
                <Button Content="Сохранить"
                       Style="{StaticResource PrimaryButton}"
                       MinWidth="100"
                       Padding="16,10"
                       IsDefault="True"
                       Click="OnSaveClick"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
