<Window x:Class="Pw.Hub.Windows.DebugVariablesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pw.Hub.Windows"
        mc:Ignorable="d"
        Title="Lua — Переменные на точке останова"
        Icon="/Assets/icon.png"
        Height="850" Width="1500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Style="{StaticResource ModernWindowChrome}">
    <Window.Resources>
        <HierarchicalDataTemplate x:Key="DebugVarTemplate" DataType="{x:Type local:DebugVarNode}"
                                  ItemsSource="{Binding Children}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="160"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Name}"/>
                <TextBlock Grid.Column="1" Text="{Binding DisplayValue}"/>
                <TextBlock Grid.Column="2" Text="{Binding TypeName}" Foreground="Gray"/>
            </Grid>
        </HierarchicalDataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{StaticResource BackgroundSecondaryBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="12,8">
            <DockPanel>
                <TextBlock x:Name="TitleText"
                           Style="{StaticResource ModernTextBlock}"
                           FontWeight="SemiBold"
                           Text="Переменные"
                           VerticalAlignment="Center"/>
            </DockPanel>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Locals" Margin="0,0,0,6" Style="{StaticResource ModernTextBlock}" FontWeight="SemiBold"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="Globals (частично)" Margin="8,0,0,6" Style="{StaticResource ModernTextBlock}" FontWeight="SemiBold"/>

            <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource Card}" Padding="4" Margin="0,0,4,0">
                <TreeView x:Name="LocalsTree"
                          ItemTemplate="{StaticResource DebugVarTemplate}"/>
            </Border>

            <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource Card}" Padding="4" Margin="4,0,0,0">
                <TreeView x:Name="GlobalsTree"
                          ItemTemplate="{StaticResource DebugVarTemplate}"/>
            </Border>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2"
                Background="{StaticResource BackgroundSecondaryBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1,1,1,0"
                Padding="12,8">
            <DockPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                    <Button Width="120" Style="{StaticResource PrimaryButton}" Click="OnCloseClick" Content="Продолжить"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>