<Window x:Class="Pw.Hub.Windows.UpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Обновление" Height="160" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" WindowStyle="ToolWindow" ShowInTaskbar="False" Topmost="True">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="TitleText" Text="Загрузка обновления..." FontSize="14" FontWeight="SemiBold"/>
        <ProgressBar x:Name="Progress" Grid.Row="1" Margin="0,10,0,0" Height="18" Minimum="0" Maximum="100"/>
        <DockPanel Grid.Row="2" Margin="0,10,0,0">
            <TextBlock x:Name="PercentText" Text="0%" DockPanel.Dock="Right"/>
            <TextBlock x:Name="StatusText" Text="Подготовка..." TextWrapping="Wrap"/>
        </DockPanel>
    </Grid>
</Window>