<Window x:Class="Pw.Hub.Windows.LuaEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
        mc:Ignorable="d"
        Title="Lua Editor" Height="700" Width="1000" WindowStartupLocation="CenterOwner">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <Button Click="OnRunClick" Padding="12,4" Margin="4">Запустить</Button>
            <Separator />
            <Button Click="OnCloseClick" Padding="12,4" Margin="4">Закрыть</Button>
            <Separator />
            <Button Click="OnClearOutputClick" Padding="12,4" Margin="4">Очистить вывод</Button>
            <Separator />
            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center">Подсказки: Ctrl+Space — автодополнение</TextBlock>
        </ToolBar>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="3*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <avalonedit:TextEditor Grid.Row="0" x:Name="Editor" ShowLineNumbers="True" FontFamily="Consolas" FontSize="14"
                                   HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                                   MinHeight="100"/>
            <GridSplitter Grid.Row="1" Height="6" HorizontalAlignment="Stretch"
                          ResizeBehavior="PreviousAndNext" ResizeDirection="Rows" ShowsPreview="True"/>
            <Border Grid.Row="2" BorderThickness="1" Padding="4" MinHeight="60">
                <DockPanel>
                    <TextBlock Text="Вывод Lua (Print):" Margin="0,0,0,4"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBox x:Name="OutputBox" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                                 FontFamily="Consolas" FontSize="13"/>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Grid>
    </DockPanel>
</Window>