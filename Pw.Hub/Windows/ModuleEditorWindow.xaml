<Window x:Class="Pw.Hub.Windows.ModuleEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Модуль" Height="520" Width="680" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="Название:"/>
            <TextBox x:Name="NameTextBox" Grid.Column="1" Margin="8,0,0,0"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="Описание:"/>
            <TextBox x:Name="DescriptionTextBox" Grid.Column="1" Margin="8,0,0,0"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="Скрипт:"/>
            <TextBox x:Name="ScriptTextBox" Grid.Column="1" Margin="8,0,8,0"/>
            <Button Grid.Column="2" Content="..." Width="30" Click="BrowseScript_Click"/>
        </Grid>

        <GroupBox Grid.Row="3" Header="Входные параметры">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" HorizontalAlignment="Right" Margin="0,0,0,6">
                    <Button x:Name="AddInputButton" Content="Добавить" Width="110" Margin="0,0,6,0" Click="AddInput_Click"/>
                    <Button x:Name="RemoveInputButton" Content="Удалить" Width="110" Click="RemoveInput_Click"/>
                </StackPanel>
                <DataGrid x:Name="InputsGrid" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Label" Binding="{Binding Label}" Width="*"/>
                        <DataGridComboBoxColumn Header="Type" SelectedItemBinding="{Binding Type}" Width="120">
                            <DataGridComboBoxColumn.ItemsSource>
                                <x:Array Type="{x:Type sys:String}">
                                    <sys:String>string</sys:String>
                                    <sys:String>number</sys:String>
                                    <sys:String>bool</sys:String>
                                </x:Array>
                            </DataGridComboBoxColumn.ItemsSource>
                        </DataGridComboBoxColumn>
                        <DataGridTextColumn Header="Default" Binding="{Binding Default}" Width="*"/>
                        <DataGridCheckBoxColumn Header="Required" Binding="{Binding Required}" Width="90"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </GroupBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Width="110" Margin="0,0,8,0" IsDefault="True" Click="Ok_Click">OK</Button>
            <Button Width="110" IsCancel="True">Отмена</Button>
        </StackPanel>
    </Grid>
</Window>